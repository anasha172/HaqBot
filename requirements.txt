# HaqBot — pinned dependencies (relaxed from the original PRD pins for
# Python 3.13 compatibility; the PRD's 2024-era pins have no 3.13 wheels).
# Everything here supports fully offline / air-gapped execution once installed.

# --- Inference / quantization (Intel OpenVINO) -------------------------------
openvino==2026.3.1
optimum-intel[openvino]==1.27.0
nncf==2.19.0

# --- RAG framework ----------------------------------------------------------
langchain==1.3.2
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-huggingface==1.2.2

# --- Embeddings / model runtime ------------------------------------------------
transformers==5.9.0
sentence-transformers==5.5.1
huggingface-hub==1.17.0
torch==2.12.0

# --- Vector store -----------------------------------------------------------
faiss-cpu==1.14.3

# --- Document parsing / ingestion --------------------------------------------
PyMuPDF==1.26.7

# --- Front-end UI ---------------------------------------------------------------
streamlit==1.61.1

# --- Core utilities -------------------------------------------------------------
numpy==2.3.3
cryptography==48.0.0        # Fernet at-rest encryption for local profile fields

# --- Testing -----------------------------------------------------------------
pytest==9.1.1
pytest-cov==7.0.0
